# ─────────────────────────────────────────────
# GradeOps — Python Dependencies
# Install: pip install -r requirements.txt
# ─────────────────────────────────────────────

# ── Core / Config ─────────────────────────────
pydantic>=2.0
pydantic-settings>=2.0        # Settings class reads from .env
python-dotenv

# ── Authentication / Security ─────────────────
python-jose[cryptography]     # JWT token encoding/decoding
passlib[bcrypt]               # password hashing
# FIX #10/#17: email-validator is required by Pydantic for EmailStr validation
email-validator>=2.0

# ── OCR ───────────────────────────────────────
pymupdf                       # PDF → image (fitz)
Pillow                        # image handling
transformers>=4.40            # Qwen2-VL / Nougat (FIX #4: Nougat now uses
                              # transformers NougatProcessor + VisionEncoderDecoderModel)
torch>=2.0                    # PyTorch
accelerate                    # HuggingFace multi-device
# nougat-ocr                  # uncomment ONLY if using the legacy nougat package directly

# ── Grading ───────────────────────────────────
langchain>=0.2
langchain-core>=0.2
langgraph>=0.1
langchain-openai              # comment out if not using OpenAI
# langchain-anthropic         # uncomment for Anthropic
# langchain-together          # uncomment for Together AI

# ── Plagiarism ────────────────────────────────
scikit-learn
numpy
# sentence-transformers       # uncomment for embedding-based similarity

# ── Storage ───────────────────────────────────
# FIX #18: boto3 is optional — only needed when STORAGE_BACKEND=s3
# boto3                       # uncomment for AWS S3 / DynamoDB

# ── Database (PostgreSQL via SQLAlchemy) ──
sqlalchemy[asyncio]>=2.0      # ORM + async core
asyncpg>=0.29                 # async PostgreSQL driver (used by SQLAlchemy)
# aiosqlite                   # uncomment for SQLite in local dev / CI

# ── API ───────────────────────────────────────
fastapi>=0.110
uvicorn[standard]
python-multipart              # UploadFile / multipart form parsing

# ── Dev / Testing ─────────────────────────────
httpx                         # async HTTP client for FastAPI TestClient
pytest
pytest-asyncio
